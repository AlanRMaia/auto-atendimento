{
  "devDependencies": {
    "cypress": "^13.2.0",
    "cypress-map": "^1.21.1",
    "cypress-xpath": "^2.0.1"
  },
  "scripts": {
    "test:todos": "npm run test:homolog && npm run test:dev",
    "homolog": "cypress open --config baseUrl=https://sitcarga-spa-homologacao.azurewebsites.net/#/ --env ENVIRONMENT=homolog",
    "test": "cypress open --config baseUrl=https://autoatendimentoteste/#/ --env ENVIRONMENT=test",
    "dev": "cypress open --config baseUrl=http://localhost:9000/#/ --env ENVIRONMENT=dev",
    "test:homolog": "cypress run --config baseUrl=https://sitcarga-spa-homologacao.azurewebsites.net/#/ --env ENVIRONMENT=homolog",
    "test:dev": "cypress run --config baseUrl=http://localhost:9000/#/ --env ENVIRONMENT=dev",
    "test:test": "cypress run --config baseUrl=http://autoatendimentoteste/#/ --env ENVIRONMENT=dev"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "@quasar/quasar-app-extension-testing-e2e-cypress": "^4.0.2",
    "cypress-get-table": "^1.0.1",
    "faker-br": "^0.4.1",
    "nodemailer": "^6.9.4"
  }
}
